This directory does not ship compiled firmware images.

Cisco OpenH264 terms that must accompany binary distributions:
`OpenH264-LICENSE.txt` (same text as `components/openh264/upstream/LICENSE`).

This repository is intended exclusively for Agent-assisted development and hardware adaptation. Give your Agent the source tree and target hardware specifications; see the top-level README and AGENTS.md.
